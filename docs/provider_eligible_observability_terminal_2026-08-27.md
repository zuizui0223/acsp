# Provider-eligible observability first activation: terminal boundary

The only authorized first activation was workflow run `33031292325`, run number 1, from commit `9f189d4cc7a6f5b9aa9b259c3c0a622e800bca11`.

Stage 1 completed and froze 6,147 discovery-candidate rows. Stage 2 then attempted the preregistered 1900–2020 historical country-facet audit for 3,161 unique species. The provider returned HTTP 429 for 29 species queries. The frozen execution contract requires a complete stage-2 audit before offline selection, so the workflow correctly terminated as `abort_not_evaluable`.

No complete 96-frame scientific object was created. Stages 3–5 were skipped, and 2021–2025 heldout outcomes, candidate patches, robust support, random comparators, recall and lift all remained unopened.

## Interpretation

The original terminal state remains authoritative. Described on the prospective two-axis vocabulary, this is `supply_status=protocol_abort`, `hypothesis_status=unavailable`, and `promotion_status=not_promoted`. This description does not retroactively reclassify the frozen run.

The 29 provider errors are a supply/evaluability failure, not evidence that the historical observability score has null or adverse prospective value. A second activation, replacement identities, a new seed, or selective retry of the 29 failed species is forbidden for this consumed protocol.

## Publication consequence

The validated Japanese 12-region candidate-patch result is unchanged. The country-framed/global extension remains unpromoted. This terminal boundary satisfies the requirement to report the observability line honestly and makes the current ACSP story complete for the Ecological Informatics route: a validated bounded product together with prospectively audited limits on automatic geographic generalization.

Methods in Ecology and Evolution would still require a separately frozen and independently confirmed end-to-end ordinary-use procedure. Journal of Applied Ecology would still require prospective field-utility evidence under comparable effort or cost.

Machine-readable provenance and all fail-closed assertions are in `validation/acsp_provider_eligible_observability_first_activation_terminal_v1.json`.
