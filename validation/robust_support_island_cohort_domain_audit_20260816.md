# Outside-Izu cohort domain audit — 2026-08-16

Status: **no occurrence outcomes inspected; no longer eligible as independent confirmation of the new domain gate**.

The previously predeclared robust-support island cohort from workflow run `31893287097` remains outcome-unconsumed: its manifest stated that occurrence coordinates had not been fetched, support envelopes had not been built, and held-out recovery had not been inspected.

However, inspection of the already declared taxon identities during the current method-development review exposed a domain-design problem: the nominal plant frame included clearly aquatic/non-terrestrial taxa, including *Zostera marina*, *Zostera caespitosa*, *Zostera asiatica*, and *Codium cylindricum*.

That identity-level observation directly motivated the current training-only domain gate. Therefore this 24-pair cohort must not later be presented as an untouched confirmation of that gate, even though its occurrence outcomes remain uninspected.

The cohort may still be used for:

- development-only checks of domain-gate mechanics;
- sampling-frame diagnostics;
- confirming that declared inapplicable taxa are handled explicitly rather than silently forced onto a terrestrial surface.

It may not be used to claim independent generalization of a method whose domain gate was designed after inspecting these taxon identities.

A final external confirmation must be sampled only after the complete procedure — domain gate, ecological support representation, training-only policy selection, set-level optimizer, and evaluation budget — is frozen. The new cohort must use a new seed and exclude all prior development/confirmation taxa according to the final protocol.

This audit does **not** consume or modify the frozen 192-pair Practical Core cohort.
